% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hdwgcna_report.R
\name{hdwgcna_module_score_violin}
\alias{hdwgcna_module_score_violin}
\title{Violin plot of one module's per-cell scores across clinical groups}
\usage{
hdwgcna_module_score_violin(
  score_df,
  module,
  trait_col = "condition",
  celltype_col = "cell_type",
  trait_groups = NULL,
  test = c("wilcox.test", "t.test"),
  ncol = 2,
  min_cells = 10,
  scales = "free_y"
)
}
\arguments{
\item{score_df}{The per-cell score table from \code{\link[=hdwgcna_module_scores]{hdwgcna_module_scores()}} (a
\verb{wgcna_<group>_modscore} target).}

\item{module}{Name of the single module column of \code{score_df} to plot.}

\item{trait_col}{Column of \code{score_df} holding the clinical group (default
\code{"condition"}, as set by \code{\link[=hdwgcna_module_scores]{hdwgcna_module_scores()}}).}

\item{celltype_col}{Column of \code{score_df} holding each cell's cell type
(default \code{"cell_type"}); used for the facets.}

\item{trait_groups}{Optional ordered clinical-group levels (as in
\code{\link[=tar_hdwgcna]{tar_hdwgcna()}}); sets the x-axis order and the pairwise comparisons. \code{NULL}
uses the sorted unique conditions.}

\item{test}{Test passed to \code{\link[ggpubr:stat_compare_means]{ggpubr::stat_compare_means()}}: \code{"wilcox.test"}
(default, unpaired Wilcoxon) or \code{"t.test"}.}

\item{ncol}{Number of facet columns (default 2).}

\item{min_cells}{Minimum cells per clinical group required for a cell type to
be shown (default 10).}

\item{scales}{Facet scales (default \code{"free_y"}, so each cell type is legible).}
}
\value{
A \code{ggplot} object (a placeholder plot with a message if there is
nothing to show).
}
\description{
Draws the per-cell \code{AddModuleScore} of a \strong{single module} as violins across
the clinical groups (x-axis, one violin + inner boxplot per group, filled by
group), \strong{faceted by cell type}. Pairwise comparisons between the clinical
groups are annotated with \code{\link[ggpubr:stat_compare_means]{ggpubr::stat_compare_means()}} (all pairs, unpaired
Wilcoxon rank-sum test by default -- an appropriate non-parametric test for
the per-cell scores). Companion to \code{\link[=hdwgcna_module_score_heatmap]{hdwgcna_module_score_heatmap()}}: the
heatmap shows every module at once, this drills into one module cell-type by
cell-type. Meant to be looped over the module columns (one tab per module).
}
\details{
Only cell types where \strong{every} displayed clinical group has at least
\code{min_cells} cells are kept, so every pairwise test is computable in every
facet (rare cell types are dropped).
}
