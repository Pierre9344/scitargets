% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scitargets_dea.R
\name{dea_report_lines}
\alias{dea_report_lines}
\title{Build Quarto child lines for one \link{scitargets_dea} comparison}
\usage{
dea_report_lines(
  x,
  fig_id,
  cluster_word = "cluster",
  obj_name = "comparison",
  heading_level = 2L,
  levels = NULL,
  meta = NULL,
  interactive_plots = NA,
  composition_state_col = NULL,
  composition_group_col = "cohort_clinic_preterrah",
  composition_patient_col = "patient_id",
  clusters_to_show = NA,
  groups_to_show = NA,
  available_clusters = NULL,
  available_groups = NULL,
  gsea_metric = c("signed_nlog10_padj", "signed_nlog10_pval", "signed_pval")
)
}
\arguments{
\item{x}{A \link{scitargets_dea} object.}

\item{fig_id}{A unique id used to build figure/table chunk labels.}

\item{cluster_word}{How to refer to the cluster in the heading (e.g.
"cluster" or "Azimuth L2 annotation").}

\item{obj_name}{Name of the variable the generated chunks reference (must be
bound in the knit environment; defaults to "comparison").}

\item{heading_level}{Markdown heading level for the comparison title.}

\item{levels}{Which differential-expression level(s) to render: \code{NULL}
(default) for all computed levels, or a subset of
\code{c("single_cell", "pseudobulk")}. Use this to split the report into a
single-cell document and a pseudobulk document.}

\item{meta}{Optional metadata data.frame (e.g. \code{seurat_obj@meta.data}) used to
build the per-replicate cell counts in the "Comparison summary" tab.}

\item{interactive_plots}{Which plot types to render interactively (ggiraph).
\code{NA} (default) renders every plot as a static ggplot2; otherwise a character
vector among \code{c("composition", "pca", "volcano", "go", "gsea")} naming the
plots to make interactive (others stay static).}

\item{composition_state_col}{Optional metadata column for the patient
cell-state composition barplot shown in the comparison header, before the
analysis tabset (e.g. "clusters_0.3" or "clinical_azimuth_l2"). \code{NULL}
(default) omits it. The generated chunk calls \code{\link[=composition_plot]{composition_plot()}} on a
\code{meta} object that must be bound in the knit environment.}

\item{composition_group_col, composition_patient_col}{Clinical-group and
patient columns for the composition plot (defaults "cohort_clinic_preterrah"
/ "patient_id").}

\item{clusters_to_show}{Display filter (default \code{NA} = all clusters). A
character vector of cluster names; the comparison is rendered only if its
cluster is listed (returns \code{character()} to skip otherwise).}

\item{groups_to_show}{Display filter (default \code{NA} = all groups). A character
vector of group names: 1 name -> any comparison involving that group; exactly
2 -> only the comparison between those two; >2 -> any comparison whose BOTH
groups are listed.}

\item{available_clusters, available_groups}{Optional full sets of valid cluster
/ group names. When supplied, a \code{clusters_to_show} / \code{groups_to_show} entry
not in these sets produces a warning callout instead of silently filtering.}

\item{gsea_metric}{Bar metric for the GSEA barplot. \code{"signed_nlog10_padj"}
(default) plots \verb{sign(NES) * -log10(adj. p-value)}, i.e. the FDR-adjusted
enrichment significance of each gene set; \code{"signed_nlog10_pval"} uses the
raw enrichment p-value and \code{"signed_pval"} the signed raw p-value. Note this
only controls the \emph{display}: the gene-level ranking fed to GSEA always uses
the raw per-gene p-value, independent of this argument.}
}
\value{
A character vector of Quarto/knitr lines.
}
\description{
Generates the markdown / knitr child text that renders a full comparison
block: a heading, then one section per computed level (single-cell, then
pseudobulk), each containing a tabset of volcano plot, DE table, GO enrichment
(per ontology and direction) and GSEA (per collection) shown side-by-side.
Pass the result to \code{\link[knitr:knit_child]{knitr::knit_child()}} with an environment in which
\code{obj_name} is bound to the object.
}
