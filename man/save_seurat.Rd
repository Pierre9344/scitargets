% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/as_seurat.R
\name{save_seurat}
\alias{save_seurat}
\title{Write a Seurat object, choosing the writer from the file extension}
\usage{
save_seurat(obj, path, nthreads = 1L)
}
\arguments{
\item{obj}{A Seurat object.}

\item{path}{Destination file. Its extension picks the writer.}

\item{nthreads}{Compression threads, passed to \code{\link[qs2:qs_save]{qs2::qs_save()}}. Ignored by
the other writers.}
}
\value{
\code{path}, invisibly, so the call can end a \code{format = "file"} target.
}
\description{
The counterpart of \code{\link[=as_seurat]{as_seurat()}}: the two agree on which extensions mean
which format, so a file written here reads back there.
}
\details{
\tabular{ll}{
   extension \tab writer \cr
   \code{.qs2}, \code{.qs} \tab \code{\link[qs2:qs_save]{qs2::qs_save()}} \cr
   \code{.rds} \tab \code{\link[=saveRDS]{saveRDS()}} \cr
   \code{.RData}, \code{.rda} \tab \code{\link[=save]{save()}}, under the name \code{obj} \cr
}


Prefer \code{.qs2}: it is markedly smaller and faster than \code{.rds} for a Seurat
object, and unlike \code{.RData} it stores a value rather than a variable name.

The parent directory is created when missing, so a caller does not have to.
}
