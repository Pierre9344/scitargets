% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scitargets_dea.R
\name{dea_comparisons_lines}
\alias{dea_comparisons_lines}
\title{Build the "DEA Comparisons" report section (all comparisons as child tabs)}
\usage{
dea_comparisons_lines(
  x,
  obj_name = "clinical_comparisons",
  heading = "DEA Comparisons",
  heading_level = 2L,
  fig_id_prefix = "cmp",
  ...
)
}
\arguments{
\item{x}{A named list of \link{scitargets_dea} objects (or anything
\code{\link[=normalize_dea_list]{normalize_dea_list()}} accepts that yields names). The names index the
chunks and label the child tabs.}

\item{obj_name}{Name of the LIST variable bound in the knit environment
(default "clinical_comparisons").}

\item{heading, heading_level}{Parent-tab heading text / markdown level (the
per-comparison tabs are nested one level below).}

\item{fig_id_prefix}{Prefix used to build each comparison's chunk-label id.}

\item{...}{Passed on to \code{\link[=dea_report_lines]{dea_report_lines()}} for every comparison (e.g.
\code{cluster_word}, \code{levels}, \code{meta}, \code{interactive_plots}, \code{clusters_to_show},
\code{groups_to_show}, \code{gsea_metric}).}
}
\value{
A character vector of Quarto/knitr lines, or \code{character()} when no
comparison is shown (e.g. all filtered out).
}
\description{
Emits one report section -- a parent tab holding every comparison's
\code{\link[=dea_report_lines]{dea_report_lines()}} block as a nested child tab -- analogous to the single
tab that \code{\link[=composition_lines]{composition_lines()}} or \code{\link[=dea_testability_lines]{dea_testability_lines()}} each produce. Call
it once instead of looping \code{\link[=dea_report_lines]{dea_report_lines()}} by hand, so the per-comparison
tabs are grouped under a single "DEA Comparisons" tab (a sibling of the
"Cell-state composition", "Testability summary", ... tabs).
}
\details{
The generated per-comparison chunks reference each object as
\code{obj_name[["<comparison name>"]]}, so only the LIST variable (\code{obj_name}) needs
to be bound in the environment passed to \code{\link[knitr:knit_child]{knitr::knit_child()}}.
}
