% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hdwgcna_report.R
\name{hdwgcna_module_composition}
\alias{hdwgcna_module_composition}
\title{Long-format module composition table}
\usage{
hdwgcna_module_composition(obj, include_grey = TRUE)
}
\arguments{
\item{obj}{An hdWGCNA Seurat object (after \code{ConstructNetwork} /
\code{ModuleEigengenes}), as produced by the \verb{wgcna_<group>} target.}

\item{include_grey}{Keep the unassigned \code{"grey"} genes (default \code{TRUE}).}
}
\value{
A \code{data.frame} with columns \code{module}, \code{color}, \code{gene} and \code{kME}.
}
\description{
Returns the gene-to-module assignment from \code{\link[hdWGCNA:GetModules]{hdWGCNA::GetModules()}} in a tidy
long format: one row per gene, with its module, the module colour and the
gene's intramodular connectivity (kME) within its assigned module. Rows are
ordered by module and then by decreasing kME, so each module's hub genes come
first. This is the table \code{\link[=tar_hdwgcna]{tar_hdwgcna()}} writes to an \code{.xlsx} file (one per
cell group) when its \code{module_table_file} argument is set.
}
