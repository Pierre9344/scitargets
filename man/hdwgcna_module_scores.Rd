% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hdwgcna_report.R
\name{hdwgcna_module_scores}
\alias{hdwgcna_module_scores}
\title{Per-cell module scores via Seurat::AddModuleScore}
\usage{
hdwgcna_module_scores(
  obj,
  trait_col = NULL,
  clustering_col = NULL,
  assay = "RNA",
  include_grey = FALSE,
  nbin = 24,
  ctrl = 100
)
}
\arguments{
\item{obj}{An hdWGCNA Seurat object (the \verb{wgcna_<group>} target), which holds
the full set of cells passed to \code{\link[=tar_hdwgcna]{tar_hdwgcna()}} plus that group's modules and
the (log-normalised) \code{assay}.}

\item{trait_col}{Optional clinical-trait metadata column; copied into the
returned \code{condition} column so the scores can be grouped by clinical
condition downstream. \code{NULL} leaves \code{condition} as \code{NA}.}

\item{clustering_col}{Optional cell-type / cluster metadata column; copied into
the returned \code{cell_type} column so each cell keeps its own cell-type label
(the scores still cover all cells). \code{NULL} leaves \code{cell_type} as \code{NA}.}

\item{assay}{Assay used for scoring (default \code{"RNA"}, log-normalised).}

\item{include_grey}{Score the \code{"grey"} module too (default \code{FALSE}).}

\item{nbin, ctrl}{Passed to \code{\link[Seurat:AddModuleScore]{Seurat::AddModuleScore()}}.}
}
\value{
A \code{data.frame} with one row per cell in the object: \code{cell},
\code{cell_type}, \code{condition}, then one numeric column per module (named after the
module). An empty \code{data.frame} if there is no non-grey module.
}
\description{
Scores \strong{every cell of the object} (all cell types, not a single-cell-type
subset) for each module of one hdWGCNA network with \code{\link[Seurat:AddModuleScore]{Seurat::AddModuleScore()}}
(the average expression of a module's genes minus a matched random control
set). The modules are those learned in one cell group, but the scores are
computed across all cells so a module's activity can be compared everywhere
and grouped by clinical condition downstream.
}
