% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scitargets_dea.R
\name{dea_gsea_yield_lines}
\alias{dea_gsea_yield_lines}
\title{Build Quarto child lines for the GSEA-yield heatmaps}
\usage{
dea_gsea_yield_lines(
  x,
  obj_name = "clinical_comparisons",
  collections = NULL,
  level = "pseudobulk",
  padj_cutoff = NULL,
  interactive = FALSE,
  fig_dims = NULL,
  heading_level = 2L,
  heading = "GSEA yield (enriched gene sets)",
  fig_id = "gsea-yield"
)
}
\arguments{
\item{x}{A list of \link{scitargets_dea} objects (used at generation time to find
the collections present at \code{level}).}

\item{obj_name}{Name of the variable the generated chunks reference (must be
bound to the same comparison list in the knit environment; default
"clinical_comparisons").}

\item{collections}{\code{NULL} (default) = every collection present across the
comparisons at \code{level}, or a character vector of collection names to
restrict to (in the given order).}

\item{level}{Differential-expression level (default "pseudobulk").}

\item{padj_cutoff}{Adjusted-p cutoff passed to \code{\link[=dea_gsea_yield_heatmap]{dea_gsea_yield_heatmap()}};
\code{NULL} uses each comparison's stored GSEA cutoff.}

\item{interactive}{Render the heatmaps interactively (ggiraph).}

\item{fig_dims}{Optional named list keyed by collection, each a named vector
or list with \code{width} and/or \code{height} (Quarto \code{fig-width}/\code{fig-height}).
Missing values fall back to 8 x 6.}

\item{heading_level}{Markdown heading level for the section title (default 2).}

\item{heading}{Section heading text.}

\item{fig_id}{A unique id used to build the figure chunk labels.}
}
\value{
A character vector of Quarto/knitr lines.
}
\description{
Quarto/knitr lines rendering one GSEA-yield heatmap per MSigDB collection (the
number of significantly enriched gene sets per cell state x comparison).
Mirrors \code{\link[=dea_upset_lines]{dea_upset_lines()}} / \code{\link[=dea_testability_lines]{dea_testability_lines()}}: pass the result to
\code{\link[knitr:knit_child]{knitr::knit_child()}} with \code{obj_name} bound (to the comparison list) in the
environment. When no GSEA was computed at \code{level}, a short note is emitted.
}
