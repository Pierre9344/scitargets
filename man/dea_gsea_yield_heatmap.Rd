% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scitargets_dea.R
\name{dea_gsea_yield_heatmap}
\alias{dea_gsea_yield_heatmap}
\title{GSEA-yield heatmap across comparisons}
\usage{
dea_gsea_yield_heatmap(
  x,
  collection,
  level = "pseudobulk",
  padj_cutoff = NULL,
  interactive = FALSE,
  width_svg = 10,
  height_svg = 6
)
}
\arguments{
\item{x}{A list of \link{scitargets_dea} objects (or anything
\code{\link[=normalize_dea_list]{normalize_dea_list()}} accepts).}

\item{collection}{MSigDB collection name (e.g. "Hallmark", "GO:BP", "C7").}

\item{level}{Differential-expression level (default "pseudobulk").}

\item{padj_cutoff}{Adjusted-p cutoff for counting a gene set as enriched;
\code{NULL} (default) uses each comparison's stored GSEA cutoff (else 0.05).}

\item{interactive}{If TRUE, return a ggiraph girafe with per-tile tooltips.}

\item{width_svg, height_svg}{girafe canvas size (interactive only).}
}
\value{
A ggplot (static) or girafe (interactive) object.
}
\description{
Heatmap of the number of significantly enriched gene sets
(\code{padj < padj_cutoff}) per (cell state x comparison) for one MSigDB
collection -- the supervisor's "GSEA-yield" summary, showing where (and how
much) pathway-level signal appeared across the comparison grid. A tile is
white (\code{NA}) when the collection was not computed for that comparison, and
grey / 0 when it was computed but no gene set passed the cutoff.
}
