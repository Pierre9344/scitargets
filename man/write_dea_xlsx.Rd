% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scitargets_dea.R
\name{write_dea_xlsx}
\alias{write_dea_xlsx}
\title{Write a \link{scitargets_dea} branch to per-level xlsx reports}
\usage{
write_dea_xlsx(
  x,
  out_dir = "./out/clinical_markers",
  levels = c("single_cell", "pseudobulk")
)
}
\arguments{
\item{x}{A \link{scitargets_dea} object, or a length-one list containing one.}

\item{out_dir}{Output directory; per-level subfolders are created under it.}

\item{levels}{Which levels to write (default both single_cell and pseudobulk).}
}
\value{
The paths to the written xlsx files.
}
\description{
Convenience wrapper for the targets pipeline: a mapped branch returns a named
list of length one, so the single object is extracted before writing. Writes
one xlsx per differential-expression level into a level subfolder of
\code{out_dir} (\code{out_dir/single_cell/...xlsx} and \code{out_dir/pseudobulk/...xlsx}),
so single-cell and pseudobulk results are kept separate.
}
