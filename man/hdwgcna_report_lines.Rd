% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hdwgcna_report.R
\name{hdwgcna_report_lines}
\alias{hdwgcna_report_lines}
\title{Build the per-cell-type hdWGCNA report section}
\usage{
hdwgcna_report_lines(
  group,
  clustering_col = "predicted.celltype.l2",
  trait_col = NULL,
  trait_groups = NULL,
  heading_level = 2L,
  enrichr_dbs = c("GO_Biological_Process_2023", "GO_Cellular_Component_2023",
    "GO_Molecular_Function_2023"),
  interactive_enrichr = TRUE
)
}
\arguments{
\item{group}{Cell-group label, exactly as passed to \code{\link[=tar_hdwgcna]{tar_hdwgcna()}} (e.g.
\code{"MAIT"} or \code{"CD4 TEM"}). The per-group target names are derived from it.}

\item{clustering_col}{Cell-type / cluster metadata column used for the
module-eigengene dot plot (default \code{"predicted.celltype.l2"}).}

\item{trait_col}{Clinical-trait metadata column (for the prose). When \code{NULL},
the module-trait and differential-module-eigengene sections are omitted.}

\item{trait_groups}{The clinical-group levels (as in \code{\link[=tar_hdwgcna]{tar_hdwgcna()}}). Used to
build the trait labels and the pairwise DME comparisons.}

\item{heading_level}{Markdown heading level for the \verb{<group> cells} title
(default 2; tabs are one level deeper, inner tabsets two deeper).}

\item{enrichr_dbs}{Enrichr databases to show, one tab each (default the GO
BP/CC/MF 2023 databases). Use \code{character()} to omit the enrichment section.}

\item{interactive_enrichr}{Whether the Enrichr barplots are interactive
(\code{ggiraph}, default \code{TRUE}).}
}
\value{
A character vector of Quarto/knitr lines.
}
\description{
Generates the Quarto/knitr lines for one cell group's hdWGCNA results --
soft-power, dendrogram + modules, module-trait correlation, Enrichr GO
enrichment, the module-eigengene dot plot and the differential module
eigengenes -- as a single tabset under a \verb{## <group> cells} heading. Mirrors
\code{\link[=dea_report_lines]{dea_report_lines()}}: emit the returned lines with \code{knitr::knit_child()} (see
that function's usage). The generated chunks read the factory targets
(\verb{wgcna_<suffix>}, \verb{_powertest}, \verb{_soft_power}, \verb{_dmes}, \verb{_enrichr}) directly
via \code{targets::tar_read()}, so nothing needs to be pre-bound in the knit
environment; the cell-type clustering column / trait must match the
\code{\link[=tar_hdwgcna]{tar_hdwgcna()}} call that produced the targets.
}
