% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scitargets_dea.R
\name{composition_plot}
\alias{composition_plot}
\title{Patient-level cell-state composition barplot}
\usage{
composition_plot(
  meta,
  state_col,
  group_col = "cohort_clinic_preterrah",
  patient_col = "patient_id",
  interactive = FALSE,
  width_svg = 12,
  height_svg = 6
)
}
\arguments{
\item{meta}{A metadata data.frame (e.g. \code{seurat_obj@meta.data}).}

\item{state_col}{Metadata column holding the cell-state / cluster annotation
(e.g. "clusters_0.3" or "clinical_azimuth_l2"). Cells with \code{NA} here are
dropped.}

\item{group_col}{Clinical-group column (default "cohort_clinic_preterrah").}

\item{patient_col}{Patient / replicate column (default "patient_id").}

\item{interactive}{If TRUE, return a ggiraph girafe with per-segment tooltips
(patient, group, state, cells, proportion); otherwise a static ggplot
(default FALSE).}

\item{width_svg, height_svg}{girafe canvas size (interactive only).}
}
\value{
A ggplot (static) or girafe (interactive) object.
}
\description{
Stacked barplot of each patient's cell-state composition (proportion of cells
per state), faceted by clinical group. Computed from a metadata data.frame
(whole dataset), so it is independent of any single comparison.
}
