% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scitargets_dea.R
\name{dea_comparisons}
\alias{dea_comparisons}
\title{Enumerate clinical-group comparisons to run}
\usage{
dea_comparisons(
  meta,
  group_by,
  cluster_by,
  groups = NA,
  clusters = NA,
  min_cells_per_group = 3L
)
}
\arguments{
\item{meta}{A metadata data.frame (e.g. \code{seurat_obj@meta.data}).}

\item{group_by}{Name of the column holding the group labels.}

\item{cluster_by}{Name of the column holding the clustering (must differ from
\code{group_by}).}

\item{groups}{Group selection: \code{NA} (default) for all pairwise comparisons,
two group names for a single pair, or one group name for one-vs-rest.}

\item{clusters}{Which clusters to use (\code{NA} = all). Use this to restrict the
analysis, e.g. keep only CD3+ T-cell Azimuth celltypes and drop Platelet /
Eryth / B cells.}

\item{min_cells_per_group}{Minimum cells per group within a cluster.}
}
\value{
A data.frame with columns comparison_name, group1, group2, cluster,
group_by, cluster_by.
}
\description{
Returns one row per (cluster x group-pair) comparison that has both groups
present (with at least \code{min_cells_per_group} cells) inside the cluster.
}
