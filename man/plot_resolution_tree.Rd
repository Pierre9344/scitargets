% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_resolution_tree.R
\name{plot_resolution_tree}
\alias{plot_resolution_tree}
\title{Plot resolution tree}
\usage{
plot_resolution_tree(
  obj,
  prefix = "clusters_",
  resolutions = NULL,
  clusters_name = NA,
  edge_label_min = 0.1,
  node_label = c("cluster_n", "n", "cluster"),
  proportion = c("child", "parent"),
  node_size = 15,
  palette = "Set2"
)
}
\arguments{
\item{obj}{A \code{data.frame} with columns representing clusterings, or a \code{Seurat} object with clustering metadata.}

\item{prefix}{Optional character value indicating the prefix used to detect clustering columns when \code{cluster_name} is not provided.}

\item{resolutions}{Optional character or numeric vector indicating the resolutions to include. Values may be supplied are prefixed using \code{prefix}.}

\item{clusters_name}{Optional character vector of explicit clustering. If provided, \code{prefix} and \code{resolution} are ignored for column discovery. The default \code{NA} triggers automatic detection.}

\item{edge_label_min}{Numeric scalar indicating the minimum proportion required to display an edge label.}

\item{node_label}{One of \code{"cluster_n"}, \code{"n"}, or \code{"cluster"}.}

\item{proportion}{One of \code{"child"}, or \code{"parent"}. Control how the edge proportions are computed.}

\item{node_size}{Numeric scalar to control the nodes size.}

\item{palette}{A character corresponding to a RColorBrewer palette name.}
}
\value{
A \code{ggplot} object
}
\description{
Build a directed tree representing how clusters split across mul
}
\details{
The function requires at least two clustering columns.

If \code{palette} is a single RColorBrewer palette name, the original palette is
used whenever possible. If more colors are needed than that palette supports,
additional colors are generated by interpolation from the palette's maximum
available colors.
}
\examples{
meta <- data.frame(
  clusters_0.2 = c("0", "0", "1", "1", "2", "2"),
  clusters_0.4 = c("0", "1", "1", "2", "3", "3"),
  clusters_0.6 = c("0", "1", "2", "2", "3", "4")
)

p <- plot_resolution_tree(meta)
p
}
