% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scitargets_dea.R
\name{dea_upset_lines}
\alias{dea_upset_lines}
\title{Build Quarto child lines for the DE-gene UpSet plots}
\usage{
dea_upset_lines(
  x,
  obj_name = "clinical_comparisons",
  directions = c("down", "up", "both"),
  level = NULL,
  comparisons = NA,
  padj_cutoff = NULL,
  fig_dims = NULL,
  heading_level = 2L,
  heading = "DE-gene overlap (UpSet)",
  fig_id = "upset"
)
}
\arguments{
\item{x}{A list of \link{scitargets_dea} objects (used at generation time to detect
whether any DE genes exist for each direction).}

\item{obj_name}{Name of the variable the generated chunks reference (must be
bound to the same comparison list in the knit environment; default
"clinical_comparisons").}

\item{directions}{Which UpSet plots to emit: any of "down", "up", "both"
(default all three).}

\item{level}{Differential-expression level passed to \code{\link[=dea_upset_plot]{dea_upset_plot()}}.}

\item{comparisons}{\code{NA} (default, all comparisons with DE genes) or a character
vector of \code{comparison_name}s to restrict to.}

\item{padj_cutoff}{Adjusted-p cutoff passed to \code{\link[=dea_upset_plot]{dea_upset_plot()}}.}

\item{fig_dims}{Optional named list keyed by direction, each a named vector or
list with \code{width} and/or \code{height} (Quarto \code{fig-width}/\code{fig-height}), e.g.
\code{list(both = c(width = 10, height = 7))}. Missing values fall back to
8 x 6.}

\item{heading_level}{Markdown heading level for the section title (default 2).}

\item{heading}{Section heading text.}

\item{fig_id}{A unique id used to build the figure chunk labels.}
}
\value{
A character vector of Quarto/knitr lines.
}
\description{
Generates Quarto/knitr lines that render one UpSet plot per requested
direction (down / up / both). Mirrors \code{\link[=dea_report_lines]{dea_report_lines()}}: pass the result to
\code{\link[knitr:knit_child]{knitr::knit_child()}} with \code{obj_name} bound (to the comparison list) in the
environment. Comparisons with no DE genes for a direction emit a short note
instead of an empty plot.
}
