% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scitargets_dea.R
\name{dea_testability_heatmap}
\alias{dea_testability_heatmap}
\title{Testability heatmap across comparisons}
\usage{
dea_testability_heatmap(
  x,
  metric = c("testable", "patients", "cells", "outliers"),
  level = "pseudobulk",
  after_removal = TRUE,
  interactive = FALSE,
  width_svg = 10,
  height_svg = 6
)
}
\arguments{
\item{x}{A list of \link{scitargets_dea} objects (or anything
\code{\link[=normalize_dea_list]{normalize_dea_list()}} accepts).}

\item{metric}{"testable" (>= min replicates per group), "patients" (min
replicates per group), "cells" (min cells per group), or "outliers" (count of
flagged outlier replicates removed). All computed from the stored PCA coords.}

\item{level}{Differential-expression level (default "pseudobulk").}

\item{after_removal}{For "testable"/"patients"/"cells": \code{TRUE} (default)
excludes the flagged outlier replicates (the post-removal view DESeq2 used);
\code{FALSE} keeps them (before removal). Ignored for \code{metric = "outliers"}.}

\item{interactive}{If TRUE, return a ggiraph girafe with per-tile tooltips.}

\item{width_svg, height_svg}{girafe canvas size (interactive only).}
}
\value{
A ggplot (static) or girafe (interactive) object.
}
\description{
Heatmap of a per-(cluster x comparison) testability metric across a list of
comparisons (the supervisor's testability summary).
}
