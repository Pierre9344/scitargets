% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hdwgcna_report.R
\name{hdwgcna_module_score_heatmap}
\alias{hdwgcna_module_score_heatmap}
\title{Heatmap of per-cell module scores grouped by clinical condition}
\usage{
hdwgcna_module_score_heatmap(
  score_df,
  trait_col = "condition",
  module_cols = NULL,
  scale_rows = TRUE,
  max_cells_per_group = 300,
  seed = 1,
  celltype_col = "cell_type",
  annotate_celltype = TRUE
)
}
\arguments{
\item{score_df}{The per-cell score table from \code{\link[=hdwgcna_module_scores]{hdwgcna_module_scores()}} (a
\verb{wgcna_<group>_modscore} target).}

\item{trait_col}{Column of \code{score_df} holding the clinical condition used to
group the cells (default \code{"condition"}, as set by \code{\link[=hdwgcna_module_scores]{hdwgcna_module_scores()}}).}

\item{module_cols}{Module columns to plot; \code{NULL} (default) uses every column
that is not \code{cell}/\code{cell_type}/\code{condition}.}

\item{scale_rows}{Z-score each module across the displayed cells (default
\code{TRUE}) so modules on different scales are comparable.}

\item{max_cells_per_group}{Down-sample to at most this many cells per clinical
condition to keep the figure legible (default 300; \code{NULL} keeps all cells).}

\item{seed}{Seed for the down-sampling (default 1).}

\item{celltype_col}{Column of \code{score_df} holding each cell's cell type, used to
order the cells and draw the cell-type annotation bar (default \code{"cell_type"}).}

\item{annotate_celltype}{Add a cell-type colour annotation bar above the heatmap
and order the cells by cell type within each clinical condition (default
\code{TRUE}). Ignored when \code{celltype_col} is missing or has a single value.}
}
\value{
A \code{ggplot} object (or a \code{patchwork} of the annotation bar + heatmap
when \code{annotate_celltype = TRUE}).
}
\description{
Tile heatmap of the per-cell module scores from \code{\link[=hdwgcna_module_scores]{hdwgcna_module_scores()}}:
rows are modules, columns are individual cells \strong{grouped (faceted) by clinical
condition}, fill is the (row-scaled) module score. Built with \code{ggplot2} for
consistency with the other report heatmaps.
}
