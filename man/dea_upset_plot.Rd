% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scitargets_dea.R
\name{dea_upset_plot}
\alias{dea_upset_plot}
\title{UpSet plot of DE-gene overlap across comparisons}
\usage{
dea_upset_plot(
  x,
  direction = c("both", "up", "down"),
  level = NULL,
  comparisons = NA,
  padj_cutoff = NULL
)
}
\arguments{
\item{x}{A list of \link{scitargets_dea} objects (e.g. \code{\link[=normalize_dea_list]{normalize_dea_list()}}
output), or anything \code{\link[=normalize_dea_list]{normalize_dea_list()}} accepts.}

\item{direction}{"both" (any DE gene), "up", or "down".}

\item{level}{Differential-expression level ("single_cell"/"pseudobulk");
\code{NULL} uses each object's default (first) level.}

\item{comparisons}{\code{NA} (default) = all comparisons that have DE genes, or a
character vector of \code{comparison_name}s to restrict to.}

\item{padj_cutoff}{Adjusted-p cutoff for calling a gene DE; \code{NULL} uses each
object's stored per-level cutoff.}
}
\value{
A ggplot (UpSet) object, or a placeholder when there are no DE genes.
}
\description{
Builds an UpSet plot (via \pkg{ggupset}) of the differentially-expressed genes
shared across comparisons, for one regulation direction.
}
