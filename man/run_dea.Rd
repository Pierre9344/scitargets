% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scitargets_dea.R
\name{run_dea}
\alias{run_dea}
\title{Run a single clinical-group differential-expression comparison}
\usage{
run_dea(
  seurat_obj,
  group_by,
  cluster_by,
  cluster,
  group1,
  group2,
  level = c("both", "single_cell", "pseudobulk"),
  run_go = TRUE,
  run_gsea = TRUE,
  go_params = list(),
  gsea_params = list(),
  pseudobulk_unit = "patient_id",
  min_cells_per_group = 3L,
  min_replicates = 3L,
  min_cells_per_sample = 10L,
  sc_min_pct = 0.1,
  sc_logfc_threshold = 0.25,
  sc_test_use = "wilcox",
  sc_only_pos = FALSE,
  pb_test = c("Wald", "LRT"),
  pb_design = "~ group",
  pb_reduced = "~ 1",
  pb_low_count_filter = c(10L, 3L),
  pb_lfc_shrink = TRUE,
  pb_pca = TRUE,
  pca_n_top_genes = 500L,
  pca_outlier_conf = 0.975,
  pb_remove_outliers = FALSE,
  assay_sc = "SCT",
  recorrect_umi = FALSE,
  species = c("human", "mouse"),
  padj_cutoff_single_cell = 0.05,
  padj_cutoff_pseudobulk = 0.05
)
}
\arguments{
\item{seurat_obj}{A Seurat object.}

\item{group_by}{Metadata column holding the group labels.}

\item{cluster_by}{Metadata column holding the clustering (must differ from
\code{group_by}); pass \code{character()} / \code{NA} to compare across all cells.}

\item{cluster}{The cluster value this comparison sits in.}

\item{group1, group2}{The two groups; \code{group2 = "rest"} means one-vs-rest.}

\item{level}{One of "both" (default), "single_cell", "pseudobulk".}

\item{run_go, run_gsea}{Whether to run the enrichment analyses (default TRUE).
GSEA needs a valid signed gene ranking, so it is computed only where that
holds (a warning is emitted, and the level's GSEA tab is omitted from the
report, otherwise):
\itemize{
\item \strong{single cell}: only when the \code{Seurat::FindMarkers} gene universe is
unfiltered, i.e. \code{sc_logfc_threshold = 0}, \code{sc_min_pct = 0} and
\code{sc_only_pos = FALSE}; genes are ranked by \code{sign(log2FC) * -log10(p_val)}.
\item \strong{pseudobulk}: only when \code{pb_test = "Wald"}; genes are ranked by the signed
DESeq2 Wald statistic (the \code{stat} column). It is skipped for \code{pb_test = "LRT"}
(the LRT statistic is unsigned).
}}

\item{go_params, gsea_params}{Named lists of GO / GSEA parameters. Pass
\code{gsea_params$pathways} (from \code{\link[=get_msigdbr_pathways]{get_msigdbr_pathways()}}) to skip re-querying.}

\item{pseudobulk_unit}{Biological replicate column for DESeq2 (default
"patient_id").}

\item{min_cells_per_group, min_replicates, min_cells_per_sample}{Quality
thresholds for the single-cell and pseudobulk levels. \code{min_replicates}
(pseudobulk only; default 3) is the minimum number of biological replicates
(\code{pseudobulk_unit}) required per group.}

\item{sc_min_pct}{Single-cell level only: minimum fraction of cells (in
either group) expressing a gene for it to be tested, passed to
\code{\link[Seurat:FindMarkers]{Seurat::FindMarkers()}} as \code{min.pct} (default 0.1).}

\item{sc_logfc_threshold}{Single-cell level only: minimum absolute log2 fold
change for a gene to be tested, passed to \code{\link[Seurat:FindMarkers]{Seurat::FindMarkers()}} as
\code{logfc.threshold} (default 0.25).}

\item{sc_test_use}{Single-cell level only: the differential-expression test
used by \code{\link[Seurat:FindMarkers]{Seurat::FindMarkers()}} (its \code{test.use}, default "wilcox").}

\item{sc_only_pos}{Single-cell level only: whether to return only
positively-regulated (up in \code{group1}) genes, passed to
\code{\link[Seurat:FindMarkers]{Seurat::FindMarkers()}} as \code{only.pos} (default FALSE = both directions).
Leave FALSE for the standard two-sided analysis; setting it TRUE drops the
down-regulated genes, so the volcano and the "down" GO/GSEA outputs are
empty for this level.}

\item{pb_test}{Pseudobulk DESeq2 test: "Wald" (default) or "LRT". Pseudobulk level only.
Use \code{"Wald"} for the standard pairwise group1-vs-group2 analysis and for downstream signed GO/GSEA.
"LRT"\verb{is intended for testing the contribution of}group\verb{relative to a reduced model; when using}"LRT"\verb{, }pb_reduced\verb{should retain all non-group covariates from}pb_design`.}

\item{pb_design}{Pseudobulk DESeq2 design as a length-1 character formula
(default "~ group"), passed through \code{\link[stats:as.formula]{stats::as.formula()}} to DESeq2. Must
reference the \code{group} term so the group1-vs-group2 contrast can be extracted.
Pseudobulk level only.}

\item{pb_reduced}{Reduced formula (length-1 character, default "~ 1") used only
when \code{pb_test = "LRT"}. Pseudobulk level only.}

\item{pb_low_count_filter}{Length-2 integer \code{c(count, n_samples)}: keep genes
whose count is \verb{>= count} in at least \code{n_samples} pseudobulk samples (default
\code{c(10, 3)}). Pseudobulk level only; does not affect the single-cell level.
Covariates referenced by \code{pb_design} (e.g. \code{~ batch + group}) are looked up
one value per biological replicate, keyed on \code{pseudobulk_unit} (and an error is
raised if a covariate is not constant within a unit).}

\item{pb_lfc_shrink}{Whether to report a shrunken log2 fold change for the
pseudobulk level (default TRUE), added as the \code{avg_log2FC_shrink} column
alongside the MLE \code{avg_log2FC}. Method cascade: apeglm (if installed) ->
ashr (if installed) -> DESeq2 built-in normal prior; \code{run_dea} prints which
method was used. Pseudobulk level only; the single-cell level is unaffected.}

\item{pb_pca}{Whether to compute a pseudobulk-sample PCA + outlier detection
over ALL samples in the cluster (default TRUE). Uses DESeq2 VST + the
top-variance genes, with outliers flagged by \link[mt:pca.outlier]{mt::pca.outlier}. Computed
independently of the DE replicate gate (a QC view) and stored on \verb{@pca}.
Pseudobulk level only.}

\item{pca_n_top_genes}{Number of top-variance VST genes used for the pseudobulk
PCA (default 500).}

\item{pca_outlier_conf}{Confidence level passed to \link[mt:pca.outlier]{mt::pca.outlier} for the
pseudobulk-sample outlier flag (default 0.975).}

\item{pb_remove_outliers}{Whether to REMOVE the PCA-flagged outlier replicates
from the pseudobulk DESeq2 computation (default FALSE = keep them). When TRUE,
outlier samples are dropped before the replicate gate, so the
\code{min_replicates} / \code{min_cells_per_sample} cutoffs are re-checked on the
post-removal sample set (the comparison becomes not-computed if too few
replicates remain). The \verb{@pca} view always retains every sample with its
outlier flag. Pseudobulk level only.}

\item{assay_sc}{Assay used for the single-cell FindMarkers test.}

\item{recorrect_umi}{Passed to \code{\link[Seurat:FindMarkers]{Seurat::FindMarkers()}} for the single-cell
level. \code{run_dea()} does NOT run \code{\link[Seurat:PrepSCTFindMarkers]{Seurat::PrepSCTFindMarkers()}} itself and no
longer subsets the object (it selects the comparison via \code{group.by}), so the
object should be \code{PrepSCTFindMarkers}-corrected upstream once and this left
at its default \code{FALSE} (reuse the already-corrected SCT counts).}

\item{species}{Organism, \code{"human"} (default) or \code{"mouse"}. Sets the GO
annotation database (org.Hs.eg.db / org.Mm.eg.db) and the GSEA msigdbr
species; stored on the object.}

\item{padj_cutoff_single_cell, padj_cutoff_pseudobulk}{Per-level adjusted-p
cutoffs (default 0.05 each). Each drives, for its level, the volcano plot's
horizontal cutoff line and the GO foreground-gene selection. The pseudobulk
cutoff is additionally passed to \code{DESeq2::results(alpha = )}, so DESeq2's
independent filtering is optimised for that FDR threshold.}
}
\value{
A \link{scitargets_dea} object.
}
\description{
Builds one \link{scitargets_dea} object for a comparison (group1 vs group2, within
\code{cluster}), running the requested differential-expression level(s) and the
downstream GO and GSEA enrichment.
}
