% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scitargets_dea.R
\name{dea_top_de_lines}
\alias{dea_top_de_lines}
\title{Build Quarto child lines for the top-DE-gene heatmap}
\usage{
dea_top_de_lines(
  x,
  seurat_obj = NULL,
  seurat_name = "seurat_merged_filtered",
  level = NULL,
  padj_cutoff = NULL,
  clusters_to_show = NA,
  groups_to_show = NA,
  group_by = NULL,
  chunk_opts = list(),
  heading_level = 2L,
  heading = "Top DE-gene heatmap",
  fig_id = "topde"
)
}
\arguments{
\item{x}{A list of \link{scitargets_dea} objects.}

\item{seurat_obj}{The Seurat object used for the cell-level heatmap, checked at
generation time; if not a Seurat object nothing is emitted.}

\item{seurat_name}{Name of the Seurat variable the generated chunk references
(must be bound in the knit environment; default "seurat_merged_filtered").}

\item{level}{Differential-expression level ("single_cell"/"pseudobulk");
\code{NULL} = each object's default.}

\item{padj_cutoff}{Adjusted-p cutoff for "DE"; \code{NULL} = each object's per-level
cutoff.}

\item{clusters_to_show, groups_to_show}{Same display filters as
\code{\link[=dea_report_lines]{dea_report_lines()}}; the heatmap pools genes only from the shown comparisons.}

\item{group_by}{Metadata column to group the heatmap cells by.}

\item{chunk_opts}{Named list of Quarto chunk options for the heatmap chunk:
\code{n_top} (number of top genes per comparison, default 20), plus \code{column}
(default "screen-outset"), \code{label}, \code{caption}, \code{fig_width}, \code{fig_height}.}

\item{heading_level, heading}{Section heading level / text (for the TOC).}

\item{fig_id}{Unique id for the chunk label.}
}
\value{
A character vector of Quarto/knitr lines (empty when nothing to show).
}
\description{
Quarto/knitr lines for a single \code{\link[=top_de_heatmap]{top_de_heatmap()}} shown AFTER all comparison
tabs. The genes are the union of the top-\code{n_top} DE genes (by |fold-change|
among significant genes) across the comparisons that the report shows (those
passing \code{clusters_to_show} / \code{groups_to_show}). Emitted only when \code{seurat_obj}
is a valid Seurat object AND at least one shown comparison has DE genes.
}
