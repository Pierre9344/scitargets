% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/as_seurat.R
\name{as_seurat}
\alias{as_seurat}
\title{Coerce a Seurat object, or a path to one, into a Seurat object}
\usage{
as_seurat(x, nthreads = 1L)
}
\arguments{
\item{x}{A Seurat object, or a single path to a file holding one.}

\item{nthreads}{Decompression threads, passed to \code{\link[qs2:qs_read]{qs2::qs_read()}}. Ignored by
the other readers.}
}
\value{
A Seurat object.
}
\description{
The target factories in this package are given the NAME of an upstream target
holding a Seurat object. That target does not always hold the object itself.
A pipeline whose merged object is too expensive to rebuild everywhere can
write it to disk and declare its target \code{format = "file"}, so that the
expensive normalisation runs once on a machine with enough memory and every
other machine reads the result. The target's VALUE is then a path, not an
object.
}
\details{
\code{as_seurat()} accepts either form, so a factory no longer has to care which
one it was handed, and a pipeline can switch a target between the two without
the downstream steps changing.

The reader is chosen from the file extension:\tabular{ll}{
   extension \tab reader \cr
   \code{.qs2}, \code{.qs} \tab \code{\link[qs2:qs_read]{qs2::qs_read()}} \cr
   \code{.rds} \tab \code{\link[base:readRDS]{base::readRDS()}} \cr
   \code{.RData}, \code{.rda} \tab \code{\link[base:load]{base::load()}} \cr
}


\code{.RData} must hold exactly ONE object. \code{save()}/\code{load()} carry the variable's
name inside the file rather than returning the value, so a file holding
several objects has no unambiguous answer to "which one is the Seurat
object?". \code{.qs2} and \code{.rds} do not have that problem and are preferred.

No caching happens here: the file is read on every call. A pipeline whose
branches each need the object should memoise on its own side rather than pay
the read per branch.
}
\examples{
\dontrun{
# Both of these give the object:
as_seurat(seurat_obj)
as_seurat("out/seurat/merged_filtered.qs2")
}
}
