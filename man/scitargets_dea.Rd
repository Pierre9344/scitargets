% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scitargets_dea.R
\name{scitargets_dea}
\alias{scitargets_dea}
\title{Differential-expression / GO / GSEA result object}
\usage{
scitargets_dea(
  comparison_name = character(0),
  group_by = character(0),
  cluster_by = character(0),
  cluster = character(0),
  groups = character(0),
  group1 = character(0),
  group2 = character(0),
  levels = character(0),
  pseudobulk_unit = character(0),
  n_cells = data.frame(),
  de = list(),
  go = list(),
  gsea = list(),
  pca = list(),
  de_params = list(),
  go_params = list(),
  gsea_params = list(),
  species = "human",
  padj_cutoffs = list(single_cell = 0.05, pseudobulk = 0.05),
  status = character(0),
  reason = character(0)
)
}
\arguments{
\item{comparison_name}{Length-1 label for the comparison ("group1::group2::cluster").}

\item{group_by}{Metadata column holding the group labels.}

\item{cluster_by}{Metadata column holding the clustering (length 0 or 1).}

\item{cluster}{The cluster value this comparison sits in.}

\item{groups}{The two group labels being compared.}

\item{group1, group2}{The two groups (\code{group2 = "rest"} means one-vs-rest).}

\item{levels}{Computed differential-expression levels (subset of
\code{c("single_cell", "pseudobulk")}).}

\item{pseudobulk_unit}{Biological-replicate column used for the pseudobulk level.}

\item{n_cells}{Per-comparison-group cell counts (\code{data.frame}).}

\item{de}{Named list (level -> standardized marker \code{data.frame}).}

\item{go}{Named list (level -> direction -> ontology -> topGO result).}

\item{gsea}{Named list (level -> collection -> fgsea result).}

\item{pca}{Named list (level -> pseudobulk VST PCA + outlier results).}

\item{de_params, go_params, gsea_params}{Parameter lists recording how each
analysis was run.}

\item{species}{Organism (\code{"human"} or \code{"mouse"}).}

\item{padj_cutoffs}{Named list of per-level adjusted-p cutoffs.}

\item{status}{Either \code{"computed"} or \code{"not_computed"}.}

\item{reason}{Length-1 explanation when \code{status == "not_computed"}.}
}
\value{
An S7 \code{scitargets_dea} object generator.
}
\description{
An S7 class holding the differential-expression results (single-cell and/or
pseudobulk), GO enrichment (one or more ontologies) and GSEA results for a
single clinical-group comparison within one cluster. Build instances with
\code{\link[=run_dea]{run_dea()}}.
}
